var x = 50;
var y = 3;
var z = x * y

// getElementById() changes the id of 'x' html element and replaces it with the assigned output
// documnet.getElementById("demo") = "Hello Javascript";

// can create javascript functions with function [name fo function] {
    // does something
// } 

// you can assign a varaibale with const = var to make a var that can't be reassigned
// you can assign a variable with let = var to make a var that is limited in scope (like a function variable)